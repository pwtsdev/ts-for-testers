/* eslint-disable no-console */
export {};

const firstName = 'Bartek';
const age = 30;
const isAdult = true;
const person = {
  name: 'Bartek Testowy',
  age: 30,
  isAdult: true,
};

console.log(firstName);
console.log(age);
console.log(isAdult);
console.log(person.name);
